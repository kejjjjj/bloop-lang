let arr = [5, 3, 1, 4, 2];

for (let i = 0; i < 4; i++) {
    for (let j = 0; j < 4; j++) {
        if (arr[j] > arr[j + 1]) {
            let temp = arr[j];
            arr[j] = arr[j + 1];
            arr[j + 1] = temp;
        }
    }
}

return arr;
