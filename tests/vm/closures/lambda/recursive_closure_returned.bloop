const main = () => {

	const make = () => {
		let x = 0;
		const rec = (n) => {
			if (n == 0) return x;
			x = x + 1;
			return rec(n - 1);
		};
		return rec;
	};

	let f = make();
	return f(4);
};

return main();