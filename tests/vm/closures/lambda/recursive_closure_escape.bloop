const main = () => {

	const make = () => {
		let x = 0;
		const rec = (n) => {
			if (n == 0) {
				const get = () => x;
				return get;
			}
			x = x + 1;
			return rec(n - 1);
		};
		return rec;
	};

	let f = make();
	let g = f(3);
	return g();
};

return main();