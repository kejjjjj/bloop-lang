const main = () => {

	let x = 0;

	const a = (n) => {
		if (n == 0) return x;
		x = x + 1;
		return b(n - 1);
	};

	//must be a normal function as arrow functions don't get hoisted
	fn b(n) {
		if (n == 0) return x;
		x = x + 1;
		return a(n - 1);
	}

	return a(4);
};

return main();