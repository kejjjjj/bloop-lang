R"(

const originalLog = std.console.log;
let logCalls = 0;

fn customLogger(t) 
{
	originalLog(std.length(t));
	logCalls++;
}

fn main() {

	
	std.console.log = customLogger;

	for(let i = 0; i <= 10; i++)
		customLogger("hello!");
	
	return [ logCalls, std.console.prop];

}


)"
